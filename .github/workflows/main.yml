name: Check the source code
on:
  pull_request:
    branches:
      - test_branch
jobs:
  test:
    name: Check the source code
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install packages
        run: npm ci
      - name: Lint
        run: npm run lint .eslintrc.yml
